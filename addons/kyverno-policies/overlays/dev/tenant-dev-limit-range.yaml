apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: tenant-dev-limit-range
spec:
  background: true
  rules:
    - name: generate-dev-limit-range
      match:
        any:
          - resources:
              kinds:
                - Namespace
              selector:
                matchLabels:
                  fleet.gitops/type: tenant
                  fleet.gitops/environment: dev
      generate:
        synchronize: true
        apiVersion: v1
        kind: LimitRange
        name: tenant-defaults
        namespace: "{{request.object.metadata.name}}"
        data:
          spec:
            limits:
              - type: Container
                defaultRequest:
                  cpu: 50m
                  memory: 64Mi
                default:
                  cpu: 500m
                  memory: 512Mi