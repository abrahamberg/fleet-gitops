apiVersion: v1
kind: Service
metadata:
  name: guestbook
  labels:
    app.kubernetes.io/name: guestbook
    app.kubernetes.io/part-of: examples
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: guestbook
  ports:
    - name: http
      port: 80
      targetPort: http