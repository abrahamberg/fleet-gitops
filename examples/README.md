# Examples

This directory contains product or workload examples that are deployed by ApplicationSets.

Platform controllers, policies, namespaces, and observability live under `addons/`, `charts/`, and `applicationsets/`. Product examples live here so they are clearly separated from the platform layer.

- `guestbook-rollout/` shows a simple product workload managed by Argo Rollouts.